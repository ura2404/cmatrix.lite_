{#
    Общая форма с логинкой
#}
{% extends "common.twig" %}

{% block style %}
    <link rel="stylesheet/less" type="text/css" href="res/res/comlogin.less"/>
{% endblock %}

{% block js %}
    <script type="module" src="res/res/comlogin.js"></script>
{% endblock %}

{% block actions %}
    {% if session.enable %}
        <a id="cm-session" class="cm-hover flex ml-2 cursor-pointer" href="{{ url.login }}" data-login-form="#cm-login">
            <span class="cm-name my-auto mr-2">{{ session.user }}</span>
            <i class="text-3xl my-auto mr-2 fas fa-user-circle"></i>
        </a>
    {% endif %}
{% endblock %}

{% block main %}
    <div id="cm-login" class="cm-noselect fixed top-0 bottom-0 left-0 right-0 bg-gray-200 bg-opacity-50 flex z-50">
        <form class="relative mx-auto my-auto flex flex-col pt-10 pb-3 px-6 sm:px-12 bg-blue-400 bg-opacity-90">
            <span class="cm-a-close cm-hover absolute p-3 top-0 right-0 cursor-pointer"><i class="text-white fas fa-times"></i></span>
            
            <input class="px-3 py-1 w-52 sm:w-72" name="user" placeholder="Логин" reuqired/>
            <span class="cm-err mb-3 h-4 text-red-600 text-sm">qwewqeqwe</span>
            
            <input class="px-3 py-1 w-52 sm:w-72" name="pass" placeholder="Пароль"/>
            <span class="cm-err mb-2 h-4 text-red-600 text-sm">qwewqeqwe</span>
            
            <button type="button" class="cm-a-submit mb-2 cm-hover mt-1 w-20 mx-auto px-3 py-1.5 text-white bg-blue-500">Войти</button>
        </from>
    </div>
{% endblock %}